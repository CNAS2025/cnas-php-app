apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: myapp-monitor
  namespace: default
spec:
  selector:
    matchLabels:
      app: php  # <-- Replace with your pod’s actual label
  podMetricsEndpoints:
    - port: http  # <-- Replace with your container port name or number
      path: /metrics
      interval: 15skubectl 